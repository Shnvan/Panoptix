# Alembic migrations

This folder contains Alembic migrations for the Panoptix control-plane Postgres schema.

Database URL comes from `DATABASE_URL` (loaded via `.env` where available) per the current project instruction to use a single connection string.

